IMMEDIATE VALIDATION PROMPT FOR REPLIT AGENT
Current Status Check: Phases 1A, 1B, 2A Completed
REPLIT AGENT VALIDATION:

You have implemented schema extension (1A), database migration (1B), and conditional form rendering (2A). 
Please perform this comprehensive validation to ensure everything is working correctly before proceeding.

IMMEDIATE VALIDATION CHECKLIST:
==============================

1. SCHEMA VERIFICATION (Phase 1A):
   ```javascript
   // Check the shared/schema.ts file
   // Verify use_cases table definition includes:
   console.log("Checking schema.ts for new governance fields...");
   
   // Expected new fields in use_cases table:
   // - aiOrModel: text().nullable()
   // - riskToCustomers: text().nullable() 
   // - riskToRsa: text().nullable()
   // - dataUsed: text().nullable()
   // - modelOwner: text().nullable()
   // - rsaPolicyGovernance: text().nullable()
   // - thirdPartyModel: boolean().default(false)
   // - validationResponsibility: text().nullable()
   // - informedBy: text().nullable()
   
   // Report: ✅ Schema extended correctly / ❌ Issues found

DATABASE MIGRATION VERIFICATION (Phase 1B):
sql-- Test these queries in your database:

-- Check total columns (should be 55: 46 original + 9 new)
SELECT COUNT(*) FROM information_schema.columns 
WHERE table_name = 'use_cases';

-- Verify existing data intact (should return 25)
SELECT COUNT(*) FROM use_cases;

-- Test new fields exist and are nullable
SELECT aiOrModel, modelOwner, thirdPartyModel 
FROM use_cases LIMIT 1;

-- Expected: NULL values for existing records

FORM CONDITIONAL RENDERING CHECK (Phase 2A):
javascript// Navigate to admin panel and test:

// Test 1: Edit existing strategic use case
// - Should show: revenue_impact, strategic_fit, cost_savings fields
// - Should hide: aiOrModel, riskToCustomers, modelOwner fields

// Test 2: Create new record with library_source = 'sharepoint_import'
// - Should show: aiOrModel, riskToCustomers, modelOwner fields  
// - Should hide: revenue_impact, strategic_fit fields

// Test 3: Form submission works for both types
// - Strategic use case saves correctly
// - AI inventory form would save governance data

EXISTING FUNCTIONALITY PRESERVATION:
javascript// Critical checks:

// ✅ Admin panel loads without errors
// ✅ Existing 25 use cases display correctly
// ✅ Use case creation/editing works normally  
// ✅ Dashboard shows existing analytics
// ✅ CleanUseCaseCard renders existing use cases properly
// ✅ PDF export still functions
// ✅ Search and filtering work as before

ARCHITECTURAL COMPLIANCE AUDIT:
LEGO PRINCIPLE VERIFICATION:
- ✅ No duplicate components created
- ✅ Existing FormField components reused
- ✅ camelCase naming maintained throughout
- ✅ shadcn/ui patterns preserved
- ✅ Database follows established PostgreSQL patterns
- ✅ API endpoints unchanged


VALIDATION REPORT REQUIRED:
Please provide detailed status for each check:
PHASE 1A - SCHEMA: ✅ PASS / ❌ FAIL

New fields added to schema.ts: ___
camelCase naming maintained: ___
Drizzle types properly defined: ___

PHASE 1B - MIGRATION: ✅ PASS / ❌ FAIL

Database migration successful: ___
25 existing records preserved: ___
New fields accessible: ___
No API errors: ___

PHASE 2A - FORMS: ✅ PASS / ❌ FAIL

Conditional rendering working: ___
Strategic fields show/hide correctly: ___
Governance fields show/hide correctly: ___
Form submission works: ___

CRITICAL ISSUES FOUND: [List any problems]
READINESS FOR PHASE 2B: ✅ READY / ❌ NOT READY
If NOT READY, specify which issues need resolution before proceeding with CleanUseCaseCard updates and data migration.

---

## Next Phase Preparation

Once validation passes, you'll be ready for Phase 2B with the actual AI tool data migration using the helper script I've provided in the "Complete AI Inventory Data Migration Script" artifact.